apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skillhub-data
  namespace: skillhub
spec:
  storageClassName: nfs-storage-192.168.13.66
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
